import IntroduceList from "../../organisms/introduce-list/introduce-list";

const SectionMeetupsList = (props) => {
  return (
    <section>
      <IntroduceList />
    </section>
  );
};

export default SectionMeetupsList;
